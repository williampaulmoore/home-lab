# container
# network
# volume
# image
alias d="docker"
alias dk_alias="alias | sed 's/^alias //' | grep dk"
alias dkcl="docker ps"
alias dkci="docker inspect"
alias dkcr="docker rm"
alias dknl="docker network ls"
alias dkni="docker network inspect"
alias dkvl="docker volume ls"
alias dkvi="docker volume inspect"
alias dkil="docker image ls"
alias dkii="docker image inspect"
alias dkir="docker image rm"

